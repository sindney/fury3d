## 1. Engine-level unique-name helper

- [x] 1.1 Create `engine/Fury/EntityUtil.h` declaring `std::string UniqueName(const std::string& base, const std::function<bool(const std::string&)>& exists);`
- [x] 1.2 Implement `UniqueName` in `engine/Fury/EntityUtil.cpp` (or inline in the header) per the suffix scheme in the `asset-unique-naming` spec: scan integers from 1 for the `(N)` form, from 2 for the `(copy N)` form, hard cap 100000, fallback `base + " (?)"`.
- [x] 1.3 Refactor `UniqueChildName` in `engine/Fury/Editor/EditorWindows.cpp:496-509` to delegate to `fury::UniqueName` with a `SceneNode::FindChild`-backed predicate. Verify behavior unchanged by re-running the editor on an existing scene and duplicating a node.
- [x] 1.4 Add `engine/Fury/EntityUtil.cpp` (if not header-only) to `engine/CMakeLists.txt` engine source list (NOT gated on `WITH_EDITOR`).

## 2. Reusable confirm dialog

- [x] 2.1 Create `engine/Fury/Editor/EditorConfirmDialog.h` declaring `void RequestConfirmDialog(const std::string& title, const std::string& message, std::function<void(bool)> onResult);` and `void RenderConfirmDialog();`.
- [x] 2.2 Implement `EditorConfirmDialog.cpp`: a `std::queue<ConfirmRequest>` pending queue, a `RenderConfirmDialog()` that opens `BeginPopupModal("ConfirmDialog", …, ImGuiWindowFlags_AlwaysAutoResize)` when the queue has an item and no dialog is currently open. Render the message, `Yes` (calls `onResult(true)` then `CloseCurrentPopup`), `No` (calls `onResult(false)` then close). Treat popup close-without-click (Esc / click-outside) as `No`.
- [x] 2.3 Wire `RenderConfirmDialog()` into `Editor::Tick` in `Editor.cpp:711` next to the other window renders.
- [x] 2.4 Add `EditorConfirmDialog.cpp` to `FURY_EDITOR_SRC` in `engine/CMakeLists.txt`.
- [ ] 2.5 Manual smoke test: call `RequestConfirmDialog("Test", "Yes/No?", [](bool y){ FURYI("got {}", y); })` from a temporary keypress — verify Yes invokes with `true`, No/Esc with `false`, exactly once each, and the queue serializes two simultaneous requests.

## 3. Generic asset picker modal

- [x] 3.1 Create `engine/Fury/Editor/EditorAssetPicker.h` declaring `void RenderAssetPickerModal(const char* popup_id, const char* title, std::type_index type, std::function<void(std::shared_ptr<void>)> onPick);`.
- [x] 3.2 Implement `EditorAssetPicker.cpp` mirroring the `RenderOpenImportModal` pattern (`Editor.cpp:373-438`): `BeginPopupModal`, a `Selectable(AllowDoubleClick)` list fed by `Scene::Active->GetEntityManager()->ForEach<T>` (dispatch by `type_index` to Mesh/Material/Texture), an `OK` button (disabled when nothing selected) that calls `onPick(selected)` + `CloseCurrentPopup`, and a `Cancel` button.
- [x] 3.3 Add `EditorAssetPicker.cpp` to `FURY_EDITOR_SRC` in `engine/CMakeLists.txt`.

## 4. Content Browser refactor — asset grid

- [x] 4.1 In `engine/Fury/Editor/Editor.h`, declare `void SelectAssetInBrowser(std::type_index type, const std::string& name);` (sets `g_SelectedAsset` + flags a "scroll to selection" request).
- [x] 4.2 Replace `static std::string g_SelectedFile` in `EditorWindows.cpp:951` with `static std::optional<std::pair<std::type_index, std::string>> g_SelectedAsset;` and `static std::optional<std::string> g_PendingScrollToAsset;`. Move these into a small `ContentBrowserState` struct if cleaner.
- [x] 4.3 Rewrite `RenderContentBrowserWindow` in `EditorWindows.cpp:963-1006` to: query `Scene::Active->GetEntityManager()`, run `ForEach<Mesh>` and `ForEach<Material>` collecting into a `std::vector<std::pair<std::type_index, void*>>` (or two typed vectors), and render a wrapping icon grid via `ImGui::BeginTable`/`ImGui::NextColumn` or a manual `ImGui::SameLine` cursor-wrap loop with tile width 80px (64 thumbnail + 16 padding). Empty scene → render `"(no assets in active scene)"`.
- [x] 4.4 Implement tile rendering: a 64×64 thumbnail area + name label row. Type badge (`M`/`Mat`) drawn in the top-left corner via `ImGui::GetWindowDrawList()->AddText`. Selection highlight via `ImGui::GetFrameBackground` color override.
- [x] 4.5 Material tile thumbnail: if `mat->GetTextures()["diffuse_texture"]` non-null → `ImGui::Image((ImTextureID)(intptr_t)tex->GetID(), ImVec2(64,64), ImVec2(0,1), ImVec2(1,0))`. Else if `mat->GetUniforms()["DIFFUSE_COLOR"]` exists → `AddRectFilled` with that color. Else → checkerboard via two `AddRectFilled` calls.
- [x] 4.6 Click handler: set `g_SelectedAsset = {type, name}`. Double-click (`ImGui::IsMouseDoubleClicked(0)` on the tile) → `ImGui::OpenPopup(("MeshEditor:" + name).c_str())` or `("MaterialEditor:" + name).c_str()` — handled by the asset-editor-windows work in section 6.
- [x] 4.7 Right-click context menu on a tile: `ImGui::BeginPopupContextItem` with `Duplicate` / `Rename` / `Delete`. Right-click on empty grid: `Refresh` item (sets a `g_NeedsRefresh` flag — actually a no-op since we re-enumerate every frame, but keep the menu item per spec).
- [x] 4.8 F2 on a selected tile: enter inline rename mode (replace label row with `ImGui::InputText` with `ImGuiInputTextFlags_EnterReturnsTrue`). On Enter: validate (empty → revert; collision → `UniqueName(newName, EntityManager::Get<T>(n) != nullptr)`); apply via `EntityManager::Remove<T>(oldName)` + `asset->SetName(newName)` + `EntityManager::Add<T>(asset)`; update `g_SelectedAsset.second` to new name. On Esc: cancel.
- [x] 4.9 Duplicate menu action: serialize the asset via `FileUtil::SerializeToString(asset->Save(...))` → `FileUtil::DeserializeFromString` into a new instance; set name via `UniqueName(origName + " (copy)", predicate)`; `EntityManager::Add<T>(copy)`. Verify the new tile appears next frame.
- [x] 4.10 Delete menu action: count references by traversing `Scene::Active->GetRootNode()` recursively and counting `MeshRender` components where `mesh.lock() == target` or any `materials[i].lock() == target`. If count > 0 → `RequestConfirmDialog("Delete Asset", "<name> is in use by <count> MeshRender(s). Delete anyway?", [asset, type, name](bool yes){ if (yes) EntityManager::Remove<T>(name); })`. If count == 0 → `EntityManager::Remove<T>(name)` directly.
- [x] 4.11 Implement `Editor::SelectAssetInBrowser` in `Editor.cpp`: set `g_SelectedAsset` and `g_PendingScrollToAsset = name`.
- [x] 4.12 In `RenderContentBrowserWindow`, after the grid renders, if `g_PendingScrollToAsset` is set and the matching tile's `ImGui::GetItemRectMin().y` is outside the visible region → `ImGui::SetScrollHereY()`. Clear the pending flag.
- [x] 4.13 Remove the old `IconForExt` helper and the `std::filesystem::directory_iterator` loop. The `g_SelectedFile` static is removed.
- [ ] 4.14 Manual test: open `Resource/Scene/scene_saved.json` (multiple meshes & materials). Verify grid renders tiles, selection works, double-click opens editors (after section 6), right-click menu works, F2 rename with collision produces `(1)` suffix, duplicate produces `(copy)`, delete with no refs removes the tile, delete with refs shows confirm dialog.

## 5. MeshRender inspector refactor — rebinding + jump-to-asset

- [x] 5.1 In `EditorNodeProperties.cpp:360-404` (`RenderMeshRenderBody`), replace the read-only mesh name summary line with a row: label `Mesh:`, name (or `(no mesh)` in disabled text), `Browse…` button, `→` (Jump-to-Asset) button. The `→` button is `ImGui::BeginDisabled` when mesh is null. On click → `Editor::SelectAssetInBrowser(typeid(Mesh), mesh->GetName())` and (optionally) `Editor::FocusContentBrowser()`.
- [x] 5.2 `Browse…` mesh button → `ImGui::OpenPopup("MeshPicker")`. Render `RenderAssetPickerModal("MeshPicker", "Pick Mesh", typeid(Mesh), [mr](shared_ptr<void> p){ auto m = std::static_pointer_cast<Mesh>(p); mr->SetMesh(m); });`.
- [x] 5.3 Replace the per-material-slot collapsing header with a row per slot: `Slot <i>:`, material name (or `(none)`), `Browse…` button (opens `"MaterialPicker<i>"` popup → `RenderAssetPickerModal` with `typeid(Material)` → `mr->SetMaterial(picked, i)`), `→` jump button (disabled when slot null), `×` remove-slot button (sets `m_Materials[i].reset()`).
- [x] 5.4 Render `Add Material Slot` button when `mr->GetMaterialCount() < mesh->GetSubMeshCount()`. On click → append a null `weak_ptr<Material>` to `m_Materials` (via a new `MeshRender::AddMaterialSlot()` method or by directly calling `SetMaterial(nullptr, count)` — pick whichever fits `MeshRender`'s API; if `SetMaterial(nullptr, …)` doesn't append a null slot, add a tiny helper).
- [x] 5.5 In the Material block's `RenderMaterialTextureRow` (`EditorNodeProperties.cpp:298-322`), add a `Browse…` button next to the texture row → opens `"TexturePicker<key>"` popup → `RenderAssetPickerModal` with `typeid(Texture)` → `mat->SetTexture(key, picked)`.
- [ ] 5.6 Manual test: select a node with MeshRender. Click `Browse…` on mesh → pick another mesh → verify mesh swaps and the node's AABB updates (visible in gizmo / selection viz). Click `→` → verify Content Browser scrolls to the tile. Click `×` on a material slot → verify slot becomes `(none)`. Click `Add Material Slot` → verify a new `(none)` slot appears.

## 6. Asset editor windows — Mesh + Material

- [x] 6.1 Create `engine/Fury/Editor/EditorAssetWindows.h` declaring `void RenderMeshEditorModal(const std::shared_ptr<Mesh>& mesh);`, `void RenderMaterialEditorModal(const std::shared_ptr<Material>& mat);`, and `void RenderAllOpenAssetEditors();` (called from `Editor::Tick`).
- [x] 6.2 Implement `EditorAssetWindows.cpp` with a `std::unordered_set<std::string> g_OpenMeshEditors;` and `g_OpenMaterialEditors;` (keyed by popup ID `"MeshEditor:<name>"` / `"MaterialEditor:<name>"`). The Content Browser's double-click handler inserts into the set; `RenderAllOpenAssetEditors` iterates and renders `BeginPopupModal` for each, removing from the set when closed.
- [x] 6.3 Material editor body: header (`name`, `opaque` checkbox, `texture_flags` hex label); Textures table reusing `RenderMaterialTextureRow` from `EditorNodeProperties.cpp` (extract that function to a shared header if it's currently file-static, or duplicate the small render block); Uniforms table iterating `mat->GetUniforms()` and dispatching by runtime uniform type (`ColorEdit4` for color uniforms, `DragFloat` for `*f` types, `DragInt` for `*i` types, read-only label for matrices); Shader passes list (read-only).
- [x] 6.4 Mesh editor body — metadata block: name, total vert/index/tri counts, submesh count, per-submesh collapsible counts, AABB min/max/size/center, `cast_shadows` checkbox bound to `Mesh::GetCastShadows` / `SetCastShadows`, joint summary if skinned.
- [x] 6.5 Mesh editor body — 3D preview pane: `ImGui::BeginChild("preview", size)`; capture content rect; render offscreen into a `Texture::GetTemporary` color RT + raw GL FBO + depth attachment (mirror `Pass::m_FrameBuffer` setup at `Pass.cpp:468-477`). Use a one-time-compiled `EditorThumbnailShader` (flat-shaded directional light). Camera: orbit (`azimuth`, `elevation`, `distance`) with state per open editor (lookup by mesh `BufferId` or popup ID). Drag → update azimuth/elevation (clamp elev `[-89°, 89°]`). Wheel → scale distance (clamp `[0.1×initial, 10×initial]`). Compute initial `distance` from `mesh->GetAABB()` radius and `0.6` fill factor.
- [x] 6.6 After offscreen render, draw the result via `ImGui::Image((ImTextureID)(intptr_t)rt->GetID(), size, ImVec2(0,1), ImVec2(1,0))`.
- [x] 6.7 ImGuizmo::ViewManipulate integration: `ImGuizmo::SetDrawlist(ImGui::GetWindowDrawList())`, `ImGuizmo::SetRect(preview rect)`, then `ImGuizmo::ViewManipulate(&view.Raw[0], 60.0f, corner_pos, ImVec2(60,60), 0)`. If `ImGuizmo::IsUsingViewManipulate()` was true this frame, decompose the modified `view` back into `azimuth`/`elevation`/`distance` for next frame (extract the camera position from the view matrix via `MathUtil::Decompose`-style logic, then convert to spherical around the AABB center).
- [x] 6.8 Add `EditorAssetWindows.cpp` to `FURY_EDITOR_SRC` in `engine/CMakeLists.txt`.
- [ ] 6.9 Manual test: double-click a Material tile → editor opens, change a uniform via `DragFloat` → verify the change persists in the scene (save & reload). Double-click a Mesh tile → editor opens with a centered preview, drag rotates, wheel zooms, click the `+X` face of the ViewManipulate cube → camera snaps to +X view. Esc closes.

## 7. Mesh thumbnail cache for Content Browser tiles

- [x] 7.1 In `EditorAssetWindows.cpp` (or a small `EditorMeshThumbnails.cpp`), implement `std::unordered_map<size_t /*BufferId*/, ThumbnailCacheEntry> g_MeshThumbnails;` where `ThumbnailCacheEntry { GLuint fbo; std::shared_ptr<Texture> colorRT; std::shared_ptr<Texture> depthRT; }`.
- [x] 7.2 Implement `ImTextureID GetMeshThumbnail(const std::shared_ptr<Mesh>& mesh, const EditorThumbnailShader& shader)`: lookup by `mesh->GetBufferId()`; if missing or `BufferId` mismatch, allocate/reallocate FBO+RTs (`Texture::GetTemporary(128,128,...)`, `glGenFramebuffers`, `glBindFramebuffer`, `glFramebufferTexture2D` for color + depth), render the mesh once with the thumbnail shader (orbit angles `(30°, 20°)`, distance from AABB radius * 0.6 fill), cache, return `colorRT->GetID()`.
- [x] 7.3 Eviction: at the start of each `RenderContentBrowserWindow` ForEach pass, build a `std::unordered_set<size_t> liveBufferIds;`; after the pass, erase cache entries whose `BufferId` is not in the set (call `glDeleteFramebuffers` + release RTs).
- [x] 7.4 Wire mesh tile thumbnail (section 4.5's mesh branch) to call `GetMeshThumbnail(mesh, shader)`.
- [ ] 7.5 Manual test: open Content Browser on a scene with several meshes — verify thumbnails render. Modify a mesh (e.g. via a script) → verify the thumbnail refreshes. Delete an asset → verify its FBO is freed (no GL leak warning in the Profiler).

## 8. Final integration & cleanup

- [x] 8.1 Remove the `Editor::SetSceneIO` `scene_dir` field from the Content Browser data path (the File menu and Open/Import modals still use it — keep it on `Editor::SceneIO`, just stop reading it from the Content Browser). Confirm no `g_SelectedFile` references remain.
- [x] 8.2 Update `examples/Editor.lua` if any callback signature changed (likely none — the Lua side is unaffected).
- [x] 8.3 Update `examples/imgui.ini` if window default sizes changed (Content Browser defaults larger to fit the grid — bump from 360×320 to 480×360 or similar).
- [ ] 8.4 Full end-to-end manual test on `Resource/Scene/scene_saved.json`: (a) browse mesh+material tiles; (b) double-click → asset editors open and edit round-trips; (c) inspector rebinds mesh+material via Browse… and `→` jumps to tile; (d) F2 rename with collision auto-suffixes; (e) Duplicate produces `(copy)`; (f) Delete on a referenced asset pops the confirm dialog and removing the asset leaves the MeshRender's weak_ptr expired (its `GetRenderable()` returns false — verify via the inspector's status indicator going red).
- [x] 8.5 Run the existing openspec-driven tests for `scene-inspector-component-panel` and `editor-shell` and update any tests that asserted the old Content Browser file-list behavior or the read-only MeshRender inspector. Add new tests covering the new tile grid, context menu, picker, jump-to-asset, and confirm dialog (mirror the existing test harness style).
- [x] 8.6 Run `openspec validate content-browser-assets` to confirm all artifacts are well-formed and apply-ready.
